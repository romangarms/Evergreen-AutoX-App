{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) \"$CLAUDE_PROJECT_DIR/.venv/bin/ruff\" check --fix --quiet \"$f\"; \"$CLAUDE_PROJECT_DIR/.venv/bin/ruff\" format --quiet \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting with ruff"
          }
        ]
      }
    ]
  }
}
